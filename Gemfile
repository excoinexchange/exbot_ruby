source 'https://rubygems.org'

gem 'excoin', git: 'https://github.com/excoinexchange/excoin_ruby_wrapper.git'
gem 'faye'
gem 'eventmachine'
gem 'json'
gem 'money'

root_dir = File.expand_path('../', __FILE__)
$LOAD_PATH.unshift(root_dir) unless $LOAD_PATH.include?(root_dir)
